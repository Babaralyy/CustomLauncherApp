<?xml version="1.0" encoding="utf-8"?>


<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
        <!-- Background track -->
        <item android:id="@android:id/background">
            <shape android:shape="rectangle">
                <solid android:color="#E0E0E0" /> <!-- Set your desired track color here -->
                <corners android:radius="20dp" /> <!-- Adjust corner radius as needed -->
            </shape>
        </item>
        <!-- Thumb track -->
        <item android:id="@android:id/secondaryProgress">
            <shape android:shape="rectangle">
                <solid android:color="#FFFFFF" /> <!-- Set your desired thumb track color here -->
                <corners android:radius="20dp" />
                <size android:height="20dp" android:width="80dp"/> <!-- Adjust corner radius to match the track -->
            </shape>
        </item>
    </layer-list>



<!--    <size android:width="80dp" android:height="30dp" />-->
<!--    <solid android:color="@color/notification_grey" />-->
<!--    <corners android:radius="20dp" />-->
